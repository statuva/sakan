import 'package:flutter/material.dart';

class FamilyTimeScreen extends StatelessWidget {
  const FamilyTimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Family Time Screen'),
      ),
    );
  }
}